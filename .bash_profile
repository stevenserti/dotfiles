#! /bin/bash

if [ -f ~/.bashrc ]
then
  . ~/.bashrc
fi

#eval `dbus-launch --sh-syntax`
#export `gnome-keyring-daemon`
#source /utils/scripts/svn_keyring.bashrc
#source /utils/scripts/svn-1.7.8.bashrc
